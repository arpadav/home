1. change instead of manual username, get USER/HOME from env
2. change debug enter to be dir location agnostic
3. create headless install of all env
4. create headless install of just aedit
5. alias eza and fd for ls and find, respectively
6. create comprehensive README.md on top-level, in `debug` folder (on how to enter docker container, how to load, and make a new script which is the curl -fsSL <url> | sh to the headless `aedit` flake), and a README in the headless `aedit` directory
